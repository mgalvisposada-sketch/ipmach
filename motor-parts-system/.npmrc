# Resolve peer dependency conflicts when using React 19 with packages that
# still declare peer deps for React 16/17/18 (e.g. @headlessui v1, lucide-react).
# See: https://docs.npmjs.com/cli/v10/using-npm/config#legacy-peer-deps
legacy-peer-deps=true
